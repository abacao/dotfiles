# auto-complete
if [ -f $(brew --prefix)/etc/bash_completion ]; then
  . $(brew --prefix)/etc/bash_completion
fi

eval "$(direnv hook bash)"

complete -C '/home/abacao/.linuxbrew/bin/aws_completer' aws
